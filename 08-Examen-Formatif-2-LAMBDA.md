<h2>Examen AWS Lambda (Lab 178 — Sales Analysis Report) — QCM (40 questions)</h2> <p><strong>Note (aide autorisée) :</strong><br> Vous pouvez consulter la documentation AWS (Lambda, IAM, EventBridge/CloudWatch Events, Systems Manager Parameter Store, VPC/Security Groups, SNS, CloudWatch Logs) pour valider les options, syntaxes et bonnes pratiques (cron AWS, permissions IAM, layers, VPC, logs, variables d’environnement, etc.).</p> <hr> <h3>PARTIE 1 — Architecture du Lab 178 (1–8)</h3> <details> <summary> PARTIE 1 — Architecture du Lab 178 (1–8) </summary> <p><strong>1) Dans le Lab 178, quel service déclenche la fonction <code>salesAnalysisReport</code> tous les jours à 20h (lun–sam) ?</strong><br> A) Cron Linux sur l’instance EC2<br> B) Amazon EventBridge (CloudWatch Events)<br> C) Amazon SNS<br> D) AWS CloudTrail</p> <p><strong>2) Dans l’architecture, pourquoi y a-t-il deux fonctions Lambda (orchestrateur + extracteur) ?</strong><br> A) Pour doubler les coûts AWS<br> B) Pour séparer responsabilités : orchestration/notification vs extraction SQL via VPC<br> C) Parce qu’une Lambda ne peut jamais appeler une autre Lambda<br> D) Parce que SNS exige deux fonctions</p> <p><strong>3) La fonction <code>salesAnalysisReportDataExtractor</code> sert principalement à…</strong><br> A) Publier un message dans SNS<br> B) Exécuter une requête SQL sur <code>cafe_db</code> et retourner les résultats<br> C) Créer un VPC<br> D) Générer un certificat TLS</p> <p><strong>4) La fonction <code>salesAnalysisReport</code> fait principalement…</strong><br> A) Lit les paramètres de connexion BD, invoque l’extracteur, formate et publie vers SNS<br> B) Héberge le site web du Café<br> C) Remplace le serveur MySQL<br> D) Crée le Security Group</p> <p><strong>5) Où sont stockées les informations sensibles de connexion BD (<code>/cafe/dbUser</code>, etc.) ?</strong><br> A) Dans le code Python en dur<br> B) Dans AWS Systems Manager Parameter Store<br> C) Dans CloudWatch Logs<br> D) Dans l’AMI EC2</p> <p><strong>6) Quel service envoie le rapport par e-mail à l’administrateur ?</strong><br> A) Amazon SNS (topic + abonnement e-mail)<br> B) Amazon S3<br> C) Amazon ECR<br> D) AWS Config</p> <p><strong>7) Quel composant reçoit l’ARN du topic SNS via variable d’environnement <code>topicARN</code> ?</strong><br> A) <code>salesAnalysisReport</code><br> B) <code>salesAnalysisReportDataExtractor</code><br> C) EventBridge<br> D) Parameter Store</p> <p><strong>8) Quel port doit être autorisé pour que l’extracteur accède à MySQL (MariaDB) sur EC2 ?</strong><br> A) 22<br> B) 80<br> C) 3306<br> D) 443</p> </details> <hr> <h3>PARTIE 2 — EventBridge Cron AWS (UTC, champs, “?”) (9–16)</h3> <details> <summary> PARTIE 2 — EventBridge Cron AWS (UTC, champs, “?”) (9–16) </summary> <p><strong>9) La syntaxe cron EventBridge utilise combien de champs ?</strong><br> A) 5 champs (comme Linux)<br> B) 6 champs (Minutes Heures Jour-du-mois Mois Jour-semaine Année)<br> C) 7 champs obligatoires<br> D) 3 champs</p> <p><strong>10) Quelle différence importante avec crontab Linux est vraie pour EventBridge ?</strong><br> A) EventBridge s’exécute sur votre VM Linux<br> B) EventBridge utilise UTC par défaut, pas l’heure locale<br> C) EventBridge ne supporte pas les jours de la semaine<br> D) EventBridge ne peut pas invoquer Lambda</p> <p><strong>11) Dans cron AWS, le symbole <code>?</code> signifie…</strong><br> A) “Réessayer en cas d’erreur”<br> B) “Valeur inconnue calculée automatiquement”<br> C) “Pas de valeur spécifique” (utilisé pour Jour-du-mois ou Jour-semaine)<br> D) “Exécuter toutes les secondes”</p> <p><strong>12) Quelle expression planifie 20h UTC du lundi au samedi (heure pile) ?</strong><br> A) <code>cron(20 0 * * MON-SAT *)</code><br> B) <code>cron(0 20 ? * MON-SAT *)</code><br> C) <code>cron(0 20 * * * )</code><br> D) <code>cron(0 20 ? * MON-SAT)</code></p> <p><strong>13) Pourquoi on met souvent <code>?</code> dans “Jour-du-mois” quand on utilise “Jour-semaine” (MON-SAT) ?</strong><br> A) Parce que AWS exige que l’un des deux (Jour-du-mois ou Jour-semaine) soit “non défini”<br> B) Parce que <code>?</code> veut dire “le 1er du mois”<br> C) Parce que sinon Lambda ne peut pas démarrer<br> D) Parce que SNS refuse le message</p> <p><strong>14) Si vous êtes à Montréal/Toronto (UTC-5 en heure normale), 20h locale correspond typiquement à…</strong><br> A) 20h UTC le même jour<br> B) 01h UTC le lendemain<br> C) 15h UTC le même jour<br> D) 05h UTC le même jour</p> <p><strong>15) Quel service “remplace le cron” dans ce Lab ?</strong><br> A) EC2 Instance Connect<br> B) EventBridge (CloudWatch Events) Schedule<br> C) S3 Lifecycle<br> D) IAM Access Analyzer</p> <p><strong>16) Pour tester rapidement un déclencheur “dans 5 minutes”, la bonne pratique est…</strong><br> A) Mettre un cron AWS au hasard et espérer<br> B) Calculer l’heure UTC actuelle puis mettre Minutes/Heures en conséquence<br> C) Modifier le code Python pour ajouter <code>sleep(300)</code><br> D) Redémarrer l’instance EC2</p> </details> <hr> <h3>PARTIE 3 — IAM Roles & Policies du Lab (17–26)</h3> <details> <summary> PARTIE 3 — IAM Roles &amp; Policies du Lab (17–26) </summary> <p><strong>17) La “relation d’approbation” (Trust relationship) des rôles Lambda doit inclure…</strong><br> A) <code>ec2.amazonaws.com</code><br> B) <code>lambda.amazonaws.com</code><br> C) <code>s3.amazonaws.com</code><br> D) <code>sns.amazonaws.com</code></p> <p><strong>18) Quelle politique permet à une Lambda d’écrire dans CloudWatch Logs (logs) ?</strong><br> A) <code>AmazonSNSFullAccess</code><br> B) <code>AWSLambdaBasicRunRole</code><br> C) <code>AmazonSSMReadOnlyAccess</code><br> D) <code>AWSLambdaRole</code></p> <p><strong>19) Dans le Lab, quelle politique permet à <code>salesAnalysisReport</code> de lire Parameter Store ?</strong><br> A) <code>AmazonSSMReadOnlyAccess</code><br> B) <code>AmazonSNSFullAccess</code><br> C) <code>AWSLambdaVPCAccessRunRole</code><br> D) <code>AmazonEC2FullAccess</code></p> <p><strong>20) Quelle politique permet à <code>salesAnalysisReport</code> d’invoquer l’autre Lambda ?</strong><br> A) <code>AWSLambdaRole</code> (lambda:InvokeFunction)<br> B) <code>AmazonSNSFullAccess</code><br> C) <code>AmazonSSMReadOnlyAccess</code><br> D) <code>AWSLambdaBasicRunRole</code></p> <p><strong>21) Quelle politique est typiquement nécessaire pour une Lambda qui doit se connecter dans un VPC (ENI) ?</strong><br> A) <code>AWSLambdaVPCAccessRunRole</code><br> B) <code>AmazonS3FullAccess</code><br> C) <code>AmazonRoute53FullAccess</code><br> D) <code>AWSCloudTrailReadOnlyAccess</code></p> <p><strong>22) Pourquoi l’extracteur a besoin d’un rôle différent de l’orchestrateur ?</strong><br> A) Parce que les noms doivent être différents<br> B) Parce que l’extracteur a besoin des permissions VPC/ENI, pas forcément l’orchestrateur<br> C) Parce que SNS impose 2 rôles<br> D) Parce que Parameter Store n’accepte qu’un rôle</p> <p><strong>23) Une erreur <code>AccessDeniedException</code> pendant un appel AWS signifie souvent…</strong><br> A) Le port 3306 est fermé<br> B) Les permissions IAM manquent (policy/rôle incorrect)<br> C) La Lambda est en Python 2.7<br> D) Le topic SNS est déjà confirmé</p> <p><strong>24) Le principe “least privilege” signifie…</strong><br> A) Donner <code>AdministratorAccess</code> pour aller plus vite<br> B) Donner uniquement les permissions nécessaires à la tâche<br> C) Ne jamais utiliser IAM<br> D) Désactiver CloudWatch Logs</p> <p><strong>25) Si CloudWatch Logs ne reçoit aucun log, une cause très fréquente est…</strong><br> A) La Lambda n’a pas d’icône<br> B) Le rôle n’a pas les permissions logs (Basic Run Role)<br> C) Le VPC est trop petit<br> D) Le runtime est “trop récent”</p> <p><strong>26) Quel élément contrôle “qui peut invoquer la Lambda” (ex: EventBridge) ?</strong><br> A) Un abonnement SNS<br> B) Une politique basée sur ressource (Resource-based policy) / permission d’invocation<br> C) Un fichier Dockerfile<br> D) Un volume EBS</p> </details> <hr> <h3>PARTIE 4 — Layers, packaging et dépendances (PyMySQL) (27–32)</h3> <details> <summary> PARTIE 4 — Layers, packaging et dépendances (PyMySQL) (27–32) </summary> <p><strong>27) Dans ce Lab, pourquoi crée-t-on une Lambda Layer <code>pymysqlLibrary</code> ?</strong><br> A) Pour remplacer IAM<br> B) Pour fournir une dépendance externe (PyMySQL) à plusieurs fonctions sans dupliquer le code<br> C) Pour ouvrir automatiquement le port 3306<br> D) Pour planifier le cron</p> <p><strong>28) Une erreur <code>No module named 'pymysql'</code> indique généralement…</strong><br> A) Un problème de DNS<br> B) La layer n’est pas attachée (ou le package est mal structuré)<br> C) Une règle EventBridge invalide<br> D) Un problème d’abonnement SNS</p> <p><strong>29) Une layer Python doit généralement contenir ses libs dans un dossier…</strong><br> A) <code>bin/</code><br> B) <code>python/</code><br> C) <code>etc/</code><br> D) <code>var/log/</code></p> <p><strong>30) Quel avantage principal apporte une layer ?</strong><br> A) Elle rend la Lambda “toujours en exécution”<br> B) Elle partage des bibliothèques entre fonctions et réduit la taille des packages de déploiement<br> C) Elle remplace CloudWatch Logs<br> D) Elle supprime la nécessité de VPC</p> <p><strong>31) Dans la configuration Lambda, “Handler” en Python correspond à…</strong><br> A) <code>nom_fichier.nom_fonction</code> (ex: <code>salesAnalysisReport.lambda_handler</code>)<br> B) Un port réseau (ex: 3306)<br> C) Le nom du topic SNS<br> D) Le nom du VPC</p> <p><strong>32) Quel runtime est utilisé dans le Lab (selon le guide) ?</strong><br> A) Python 2.7<br> B) Python 3.9<br> C) Python 3.5<br> D) Java 6</p> </details> <hr> <h3>PARTIE 5 — VPC, Security Groups et erreurs de connexion (33–38)</h3> <details> <summary> PARTIE 5 — VPC, Security Groups et erreurs de connexion (33–38) </summary> <p><strong>33) Pourquoi l’extracteur doit être configuré dans le VPC du Café ?</strong><br> A) Pour publier dans SNS<br> B) Pour accéder réseau à l’instance EC2 LAMP (MySQL) dans le VPC<br> C) Pour lire Parameter Store<br> D) Pour utiliser EventBridge</p> <p><strong>34) Quand une Lambda est attachée à un VPC, AWS doit créer…</strong><br> A) Une AMI<br> B) Une ENI (Elastic Network Interface)<br> C) Un bucket S3<br> D) Un certificat ACM</p> <p><strong>35) La politique IAM qui permet de gérer ces ENI est typiquement…</strong><br> A) <code>AmazonSNSFullAccess</code><br> B) <code>AWSLambdaVPCAccessRunRole</code><br> C) <code>AmazonSSMReadOnlyAccess</code><br> D) <code>AWSLambdaRole</code></p> <p><strong>36) L’erreur “Task timed out after 3.00 seconds” dans ce Lab est souvent causée par…</strong><br> A) Un port MySQL (3306) non autorisé dans le Security Group / Lambda ne joint pas la BD<br> B) Un abonnement SNS confirmé trop vite<br> C) Une instance EC2 trop grande<br> D) Un bucket S3 vide</p> <p><strong>37) Pour corriger le problème principal de connexion BD, on doit vérifier…</strong><br> A) La couleur du thème AWS Console<br> B) Les règles entrantes du Security Group : autoriser TCP 3306 depuis la bonne source<br> C) Les tags du VPC uniquement<br> D) Le nom du fichier ZIP</p> <p><strong>38) Augmenter le timeout Lambda peut aider car…</strong><br> A) Ça ouvre automatiquement les ports réseau<br> B) Ça laisse plus de temps à la fonction pour établir la connexion et finir le traitement<br> C) Ça supprime IAM<br> D) Ça rend SNS inutile</p> </details> <hr> <h3>PARTIE 6 — Parameter Store, variables d’environnement, SNS (39–44)</h3> <details> <summary> PARTIE 6 — Parameter Store, variables d’environnement, SNS (39–44) </summary> <p><strong>39) Quel appel AWS permet typiquement de récupérer un paramètre Parameter Store en code ?</strong><br> A) <code>sns.publish()</code><br> B) <code>ssm.get_parameter()</code><br> C) <code>ec2.run_instances()</code><br> D) <code>route53.change_resource_record_sets()</code></p> <p><strong>40) Pourquoi stocker <code>dbPassword</code> dans Parameter Store est une bonne pratique ?</strong><br> A) Parce que c’est plus lent<br> B) Pour éviter de coder en dur des secrets dans le code et centraliser la configuration<br> C) Parce que CloudWatch Logs chiffre automatiquement les mots de passe<br> D) Parce que SNS exige Parameter Store</p> <p><strong>41) Dans le Lab, l’ARN du topic SNS est injecté via…</strong><br> A) Un fichier texte sur EC2<br> B) Une variable d’environnement <code>topicARN</code><br> C) Un port ouvert 8501<br> D) Un Security Group sortant</p> <p><strong>42) Pour recevoir un e-mail SNS, l’abonnement doit être…</strong><br> A) Supprimé puis recréé<br> B) Confirmé via un lien de confirmation reçu par e-mail<br> C) Toujours “confirmé automatiquement”<br> D) Validé par un redémarrage EC2</p> <p><strong>43) La fonction publie le rapport dans SNS via…</strong><br> A) <code>sns.publish(TopicArn=..., Message=...)</code><br> B) <code>ssm.put_parameter()</code><br> C) <code>ec2.authorize_security_group_ingress()</code><br> D) <code>lambda.create_function()</code></p> <p><strong>44) Quel élément est le meilleur endroit pour stocker une config non-secrète (ex: ARN topic) ?</strong><br> A) En dur dans le code Python<br> B) Variables d’environnement Lambda<br> C) Dans les logs CloudWatch<br> D) Dans le nom du fichier ZIP</p> </details> <hr> <h3>PARTIE 7 — CloudWatch Logs & lecture des sorties (START/END/REPORT) (45–50)</h3> <details> <summary> PARTIE 7 — CloudWatch Logs &amp; lecture des sorties (45–50) </summary> <p><strong>45) Les mots-clés <code>START</code>, <code>END</code>, <code>REPORT</code> apparaissent dans…</strong><br> A) Les logs CloudWatch d’une invocation Lambda<br> B) Le code source Python automatiquement<br> C) Les règles du Security Group<br> D) Parameter Store</p> <p><strong>46) La ligne <code>REPORT</code> contient typiquement…</strong><br> A) Le mot de passe de la BD<br> B) La durée, la mémoire allouée, la mémoire utilisée, la durée d’init, etc.<br> C) Le code SQL exact de la requête uniquement<br> D) Le nom de l’utilisateur AWS connecté</p> <p><strong>47) “Duration” vs “Billed Duration” signifie…</strong><br> A) Deux fois la même chose<br> B) Duration = durée mesurée ; Billed Duration = durée facturée (arrondie selon AWS)<br> C) Billed Duration = temps réseau uniquement<br> D) Duration = temps d’arrêt EC2</p> <p><strong>48) Si le test affiche “Execution result: failed” la première action utile est…</strong><br> A) Supprimer le VPC<br> B) Lire les logs CloudWatch de l’invocation<br> C) Désinstaller AWS CLI<br> D) Renommer le topic SNS</p> <p><strong>49) Une erreur “Task timed out after 3.00 seconds” est visible…</strong><br> A) Dans les règles DNS de Route 53<br> B) Dans le résultat d’exécution + logs CloudWatch<br> C) Dans le panneau “Billing” uniquement<br> D) Uniquement dans l’éditeur de code local</p> <p><strong>50) Un test Lambda réussi renvoie souvent un objet JSON contenant…</strong><br> A) <code>statusCode</code> et <code>body</code><br> B) <code>sshPort</code> et <code>rootPassword</code><br> C) <code>vpcId</code> et <code>eniId</code> uniquement<br> D) <code>domainName</code> et <code>zoneId</code></p> </details>
