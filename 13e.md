# Projet — Système expert de gestion intelligente des tâches récurrentes et rappels mensuels

## 1. Titre du projet

**Développement d’un système expert de gestion intelligente des tâches récurrentes avec calendrier, backend, base de données et règles de décision**

---

## 2. Contexte général

Dans la vie réelle, plusieurs obligations reviennent chaque mois : payer une facture d’électricité, payer Internet, payer un loyer, respecter une entente de paiement avec le gouvernement, renouveler une assurance, envoyer un rapport mensuel ou effectuer une tâche administrative.

Le problème est que ces tâches peuvent être oubliées. Lorsqu’une tâche est oubliée ou non complétée à temps, elle devient en retard. Une application intelligente peut aider l’utilisateur à mieux gérer ses obligations en affichant les tâches dans un calendrier et en appliquant automatiquement des règles de décision.

Dans ce projet, vous devez développer une application complète qui permet de gérer des tâches récurrentes. Le projet doit être présenté comme un **système expert simple**, car l’application devra appliquer des règles de type `IF / ELSE` pour analyser les tâches et décider automatiquement de leur état.

Par exemple :

```text
IF la date de la tâche est dépassée
AND la tâche n’est pas complétée
THEN la tâche devient en retard
AND elle s’affiche en rouge
```

Le projet doit donc combiner :

* une interface utilisateur;
* un calendrier visuel;
* un backend;
* une base de données;
* des opérations CRUD;
* un moteur de règles;
* une logique de décision de type système expert.

---

## 3. Objectif général du projet

L’objectif du projet est de développer une application web permettant de créer, consulter, modifier, supprimer et suivre des tâches récurrentes à partir d’un calendrier intelligent.

L’application doit permettre à l’utilisateur de définir une tâche récurrente, par exemple :

```text
Payer la facture mensuelle d’électricité chaque 15 du mois
du 15 juin 2026 au 15 juin 2027.
```

Le backend doit ensuite générer automatiquement toutes les tâches mensuelles correspondantes et les enregistrer dans la base de données.

Le système doit également appliquer automatiquement des règles pour déterminer si chaque tâche est :

* normale;
* urgente;
* en retard;
* complétée;
* annulée;
* archivée.

---

## 4. Idée principale du projet

Le frontend ne doit pas seulement afficher des tâches créées manuellement.

Le cœur du projet doit être dans le backend.

Le backend doit être responsable de :

* recevoir les informations saisies par l’utilisateur;
* valider les données;
* créer une entente ou une tâche récurrente;
* générer automatiquement les tâches mensuelles;
* sauvegarder les tâches dans la base de données;
* appliquer les règles du système expert;
* retourner au frontend les tâches avec leur état calculé;
* mettre à jour les couleurs à afficher dans le calendrier.

Le frontend doit principalement servir à :

* afficher les données;
* permettre la saisie;
* afficher le calendrier;
* appeler l’API du backend;
* montrer les résultats calculés par le backend.

---

## 5. Exemple obligatoire à implémenter

L’application doit obligatoirement contenir un exemple complet avec une tâche récurrente.

### Exemple 1 — Facture mensuelle d’électricité

L’utilisateur veut créer une tâche récurrente :

```text
Titre : Payer la facture mensuelle d’électricité
Description : Paiement mensuel de la facture d’électricité.
Montant : 120 $
Date de début : 15 juin 2026
Date de fin : 15 juin 2027
Jour du mois : 15
Fréquence : Mensuelle
Statut initial : Active
```

Le backend doit générer automatiquement les tâches suivantes :

```text
15 juin 2026  -> Payer la facture mensuelle d’électricité
15 juillet 2026 -> Payer la facture mensuelle d’électricité
15 août 2026 -> Payer la facture mensuelle d’électricité
15 septembre 2026 -> Payer la facture mensuelle d’électricité
15 octobre 2026 -> Payer la facture mensuelle d’électricité
15 novembre 2026 -> Payer la facture mensuelle d’électricité
15 décembre 2026 -> Payer la facture mensuelle d’électricité
15 janvier 2027 -> Payer la facture mensuelle d’électricité
15 février 2027 -> Payer la facture mensuelle d’électricité
15 mars 2027 -> Payer la facture mensuelle d’électricité
15 avril 2027 -> Payer la facture mensuelle d’électricité
15 mai 2027 -> Payer la facture mensuelle d’électricité
15 juin 2027 -> Payer la facture mensuelle d’électricité
```

Chaque tâche générée doit être enregistrée dans la base de données.

---

## 6. Autres exemples possibles de tâches récurrentes

Les étudiants peuvent ajouter d’autres exemples dans leur application.

Exemples :

```text
Payer la facture mensuelle d’Internet.
Payer le loyer.
Payer une assurance automobile.
Payer une carte de crédit.
Payer une entente avec le gouvernement.
Payer un abonnement mensuel.
Faire une sauvegarde mensuelle des données.
Envoyer un rapport mensuel.
Faire une vérification administrative.
Renouveler un service.
Planifier un rendez-vous mensuel.
Vérifier une facture.
Envoyer un paiement à une date fixe.
```

Chaque exemple doit être géré par le backend avec une logique de génération automatique.

---

## 7. Nature du système expert

Ce projet n’est pas un système expert complexe avec intelligence artificielle avancée.

Il s’agit d’un **système expert à base de règles**.

Un système expert à base de règles prend des décisions à partir de conditions.

Dans ce projet, les règles seront écrites avec des conditions simples :

```text
IF condition
THEN décision
```

ou :

```text
IF condition
AND autre condition
THEN action
```

Le système doit analyser les tâches et décider automatiquement de leur état.

---

## 8. Règles obligatoires du système expert

Le backend doit contenir un moteur de règles.

Ce moteur doit analyser les tâches enregistrées dans la base de données.

### Règle 1 — Tâche en retard

```text
IF date_de_la_tâche < date_actuelle
AND statut != "complétée"
THEN état_expert = "en retard"
AND couleur = "rouge"
```

Explication :

Si la date prévue est déjà passée et que l’utilisateur n’a pas marqué la tâche comme complétée, la tâche doit être considérée comme en retard.

---

### Règle 2 — Tâche urgente

```text
IF date_de_la_tâche >= date_actuelle
AND date_de_la_tâche <= date_actuelle + 7 jours
AND statut != "complétée"
THEN état_expert = "urgent"
AND couleur = "orange"
```

Explication :

Si une tâche arrive dans les sept prochains jours, elle doit être affichée comme urgente.

---

### Règle 3 — Tâche normale

```text
IF date_de_la_tâche > date_actuelle + 7 jours
AND statut != "complétée"
THEN état_expert = "normal"
AND couleur = "bleu"
```

Explication :

Si la tâche est encore loin dans le futur, elle est normale.

---

### Règle 4 — Tâche complétée

```text
IF statut == "complétée"
THEN état_expert = "terminée"
AND couleur = "vert"
```

Explication :

Si l’utilisateur a marqué la tâche comme complétée, elle doit rester en vert, même si la date est passée.

---

### Règle 5 — Tâche annulée

```text
IF statut == "annulée"
THEN état_expert = "annulée"
AND couleur = "gris"
```

Explication :

Une tâche annulée ne doit plus être considérée comme urgente ou en retard.

---

### Règle 6 — Entente terminée

```text
IF date_fin_entente < date_actuelle
THEN statut_entente = "terminée"
```

Explication :

Si toutes les dates de l’entente sont passées, l’entente peut être considérée comme terminée.

---

### Règle 7 — Suppression d’une entente

```text
IF utilisateur_supprime_entente == true
THEN supprimer ou archiver toutes les tâches associées
```

Explication :

Lorsqu’une entente est supprimée, les tâches générées à partir de cette entente doivent aussi être supprimées ou archivées.

---

## 9. Fonctionnalités obligatoires

## 9.1 Création d’une tâche récurrente

L’utilisateur doit pouvoir créer une tâche récurrente à partir d’un formulaire.

Le formulaire doit contenir au minimum :

```text
Titre
Description
Montant
Date de début
Date de fin
Jour du mois
Fréquence
Statut
```

Exemple :

```text
Titre : Payer la facture mensuelle d’électricité
Description : Paiement obligatoire chaque mois.
Montant : 120 $
Date de début : 2026-06-15
Date de fin : 2027-06-15
Jour du mois : 15
Fréquence : mensuelle
Statut : active
```

Après la soumission du formulaire, le frontend doit envoyer les données au backend.

Le backend doit créer l’entente et générer les tâches mensuelles.

---

## 9.2 Génération automatique des tâches par le backend

Le backend ne doit pas seulement enregistrer une seule tâche.

Il doit générer toutes les tâches comprises entre la date de début et la date de fin.

Exemple :

```text
Date de début : 15 juin 2026
Date de fin : 15 juin 2027
Fréquence : mensuelle
```

Le backend doit générer 13 tâches.

Chaque tâche doit être sauvegardée dans la table des tâches ou rappels.

---

## 9.3 Affichage dans un calendrier

L’application doit afficher un grand calendrier.

Le calendrier doit permettre de voir :

* les tâches du mois;
* les tâches passées;
* les tâches futures;
* les tâches urgentes;
* les tâches en retard;
* les tâches complétées;
* les tâches annulées.

Chaque tâche doit être affichée avec une couleur selon son état.

Couleurs minimales attendues :

```text
Rouge : en retard
Orange : urgent
Vert : complétée
Bleu : normale
Gris : annulée ou archivée
```

---

## 9.4 Modification d’une tâche

L’utilisateur doit pouvoir modifier une tâche.

Il peut modifier par exemple :

```text
Le titre
La description
Le montant
La date
Le statut
```

Attention : si une tâche est modifiée, le backend doit réappliquer les règles du système expert.

---

## 9.5 Modification d’une entente

L’utilisateur doit pouvoir modifier une entente complète.

Par exemple, il peut changer :

```text
Le montant mensuel
La date de fin
Le jour du mois
Le titre
La description
```

Le système doit gérer correctement les tâches déjà générées.

Deux approches sont acceptées :

### Approche 1 — Régénération complète

Le backend supprime les anciennes tâches non complétées et génère une nouvelle série.

### Approche 2 — Mise à jour partielle

Le backend conserve les tâches déjà complétées et met à jour seulement les tâches futures.

Les étudiants doivent expliquer l’approche choisie dans leur README.

---

## 9.6 Suppression d’une tâche

L’utilisateur doit pouvoir supprimer une tâche individuelle.

Exemple :

```text
Supprimer uniquement la tâche du 15 août 2026.
```

La suppression doit être sauvegardée dans la base de données.

---

## 9.7 Suppression ou archivage d’une entente

L’utilisateur doit pouvoir supprimer ou archiver une entente complète.

Exemple :

```text
L’ancienne entente avec le gouvernement est terminée.
Une nouvelle entente commence.
L’utilisateur veut archiver ou supprimer l’ancienne entente.
```

Si une entente est supprimée, le backend doit gérer les tâches associées.

---

## 9.8 Marquer une tâche comme complétée

L’utilisateur doit pouvoir cliquer sur une tâche et la marquer comme complétée.

Après cette action :

```text
statut = "complétée"
état_expert = "terminée"
couleur = "vert"
```

La modification doit être enregistrée dans la base de données.

---

## 9.9 Liste des tâches en retard

L’application doit afficher une section spéciale :

```text
Tâches en retard
```

Cette section doit lister uniquement les tâches dont l’état expert est :

```text
en retard
```

---

## 9.10 Liste des tâches urgentes

L’application doit afficher une section spéciale :

```text
Tâches urgentes
```

Cette section doit lister uniquement les tâches qui arrivent bientôt.

---

## 10. Architecture obligatoire

Le projet doit être organisé selon une architecture simple en trois parties.

```text
Frontend  <---->  Backend API  <---->  Base de données
```

### Frontend

Responsable de :

```text
Afficher l’interface
Afficher le calendrier
Afficher les tâches
Envoyer les actions de l’utilisateur au backend
```

### Backend

Responsable de :

```text
Recevoir les requêtes
Valider les données
Créer les ententes
Générer les tâches
Appliquer les règles du système expert
Communiquer avec la base de données
Retourner les résultats au frontend
```

### Base de données

Responsable de :

```text
Sauvegarder les ententes
Sauvegarder les tâches
Sauvegarder les statuts
Sauvegarder les états calculés
```

---

## 11. Technologies autorisées

Les étudiants peuvent choisir leur stack technologique.

### Backend possible

```text
Node.js avec Express
Python avec FastAPI
Java avec Spring Boot
C# avec ASP.NET Core
PHP avec Laravel
```

### Frontend possible

```text
HTML, CSS, JavaScript
React
Vue.js
Angular
Flutter Web
```

### Base de données possible

```text
SQLite
PostgreSQL
MySQL
Supabase
MongoDB
```

Pour ce projet, une base relationnelle est recommandée, car les ententes et les tâches ont une relation claire.

---

## 12. Modèle de base de données minimal

Le projet doit contenir au minimum deux tables principales.

---

## 12.1 Table agreements

Cette table représente les ententes ou tâches récurrentes principales.

```text
id
title
description
amount
start_date
end_date
day_of_month
frequency
status
created_at
updated_at
```

Exemple de données :

```text
id : 1
title : Payer la facture mensuelle d’électricité
description : Paiement mensuel de la facture d’électricité.
amount : 120.00
start_date : 2026-06-15
end_date : 2027-06-15
day_of_month : 15
frequency : monthly
status : active
```

---

## 12.2 Table reminders

Cette table représente les tâches générées automatiquement.

```text
id
agreement_id
title
description
amount
due_date
status
expert_state
color
created_at
updated_at
```

Exemple de données :

```text
id : 1
agreement_id : 1
title : Payer la facture mensuelle d’électricité
amount : 120.00
due_date : 2026-06-15
status : pending
expert_state : normal
color : blue
```

---

## 12.3 Relation entre les tables

Une entente peut générer plusieurs tâches.

```text
agreements 1 ---- N reminders
```

Cela signifie qu’une seule entente peut être liée à plusieurs rappels.

---

## 13. API backend obligatoire

Le backend doit fournir une API claire.

Les routes suivantes sont attendues.

---

## 13.1 Routes pour les ententes

```text
GET /agreements
```

Retourne toutes les ententes.

```text
GET /agreements/:id
```

Retourne une entente précise.

```text
POST /agreements
```

Crée une nouvelle entente et génère automatiquement les tâches.

```text
PUT /agreements/:id
```

Modifie une entente.

```text
DELETE /agreements/:id
```

Supprime ou archive une entente.

---

## 13.2 Routes pour les tâches

```text
GET /reminders
```

Retourne toutes les tâches.

```text
GET /reminders/:id
```

Retourne une tâche précise.

```text
GET /reminders/overdue
```

Retourne les tâches en retard.

```text
GET /reminders/upcoming
```

Retourne les tâches urgentes ou à venir.

```text
PUT /reminders/:id
```

Modifie une tâche.

```text
PUT /reminders/:id/complete
```

Marque une tâche comme complétée.

```text
DELETE /reminders/:id
```

Supprime une tâche.

---

## 13.3 Route pour le système expert

```text
POST /expert-rules/apply
```

Cette route applique les règles du système expert sur toutes les tâches.

Elle doit :

```text
Lire les tâches depuis la base de données
Comparer les dates avec la date actuelle
Vérifier les statuts
Mettre à jour l’état expert
Mettre à jour la couleur
Sauvegarder les résultats
Retourner les tâches mises à jour
```

---

## 14. Exemple de réponse JSON attendue

Le backend peut retourner une tâche sous cette forme :

```json
{
  "id": 1,
  "agreement_id": 1,
  "title": "Payer la facture mensuelle d’électricité",
  "amount": 120.00,
  "due_date": "2026-07-15",
  "status": "pending",
  "expert_state": "overdue",
  "color": "red"
}
```

Le frontend doit utiliser cette réponse pour afficher la tâche dans le calendrier.

---

## 15. Logique backend attendue

Le backend doit contenir au minimum trois parties importantes.

### 15.1 Service de génération des tâches

Ce service reçoit une entente et génère les rappels mensuels.

Pseudo-code :

```text
recevoir date_debut
recevoir date_fin
recevoir jour_du_mois

date_courante = date_debut

WHILE date_courante <= date_fin
    créer une tâche avec date_courante
    ajouter un mois à date_courante
END WHILE
```

---

### 15.2 Service du système expert

Ce service applique les règles.

Pseudo-code :

```text
POUR chaque tâche dans la base de données

    IF statut == "complétée"
        état = "terminée"
        couleur = "vert"

    ELSE IF statut == "annulée"
        état = "annulée"
        couleur = "gris"

    ELSE IF date_tâche < date_actuelle
        état = "en retard"
        couleur = "rouge"

    ELSE IF date_tâche <= date_actuelle + 7 jours
        état = "urgent"
        couleur = "orange"

    ELSE
        état = "normal"
        couleur = "bleu"

    sauvegarder état et couleur

FIN POUR
```

---

### 15.3 Service CRUD

Ce service permet :

```text
Créer
Lire
Modifier
Supprimer
```

les ententes et les tâches.

---

## 16. Exigences concernant le frontend

L’interface doit être claire et facile à utiliser.

Elle doit contenir au minimum les pages ou sections suivantes :

```text
Page d’accueil
Formulaire d’ajout d’une entente
Liste des ententes
Calendrier des tâches
Liste des tâches en retard
Liste des tâches urgentes
Page ou fenêtre de détail d’une tâche
Boutons modifier, supprimer, compléter
```

---

## 17. Détail du calendrier

Le calendrier doit afficher les tâches sous forme visuelle.

Pour chaque tâche, on doit voir :

```text
Titre
Date
Montant
Statut
Couleur
```

Exemple :

```text
15 juillet 2026
Payer la facture mensuelle d’électricité
120 $
En attente
Rouge si en retard
```

Le calendrier doit être suffisamment clair pour que l’utilisateur puisse repérer rapidement les tâches importantes.

---

## 18. Contraintes importantes

Les contraintes suivantes sont obligatoires :

```text
Le projet doit utiliser un backend.
Le projet doit utiliser une base de données.
Les tâches doivent être générées par le backend.
Les règles IF / ELSE doivent être appliquées dans le backend.
Le frontend ne doit pas décider seul de la couleur des tâches.
Le frontend doit recevoir l’état et la couleur depuis l’API.
Les opérations CRUD doivent être fonctionnelles.
Le calendrier doit afficher les tâches.
Les données doivent persister après fermeture de l’application.
```

---

## 19. Ce qui n’est pas accepté

Les éléments suivants ne sont pas acceptés comme solution complète :

```text
Une simple maquette sans backend.
Une application qui stocke tout seulement dans le localStorage.
Un calendrier sans base de données.
Des tâches ajoutées uniquement à la main sans génération automatique.
Une couleur décidée uniquement par le frontend.
Un projet sans règles IF / ELSE.
Un projet sans CRUD.
Un projet sans README.
Un projet impossible à exécuter.
```

---

## 20. Déploiement

Le déploiement est recommandé.

Les étudiants peuvent utiliser une plateforme de leur choix.

Exemples de plateformes possibles :

```text
Vercel pour le frontend
Netlify pour le frontend
Render pour le backend
Railway pour le backend
Supabase pour la base de données
GitHub Pages pour un frontend statique
```

Les étudiants doivent expliquer dans le README :

```text
Où se trouve le frontend
Où se trouve le backend
Où se trouve la base de données
Comment les trois parties communiquent
```

Si le projet n’est pas déployé, les étudiants doivent au minimum fournir des instructions claires pour l’exécuter localement.

---

## 21. Exemple d’architecture attendue

```text
Utilisateur
   |
   v
Interface Web
   |
   v
API Backend
   |
   v
Base de données
```

Exemple plus détaillé :

```text
React / HTML / Vue
        |
        | appels HTTP
        v
FastAPI / Express / Spring Boot
        |
        | requêtes SQL
        v
PostgreSQL / MySQL / SQLite / Supabase
```

---

## 22. Exemple de workflow utilisateur

### Étape 1

L’utilisateur ouvre l’application.

### Étape 2

Il clique sur :

```text
Ajouter une tâche récurrente
```

### Étape 3

Il saisit :

```text
Titre : Payer la facture mensuelle d’électricité
Montant : 120 $
Date de début : 15 juin 2026
Date de fin : 15 juin 2027
Jour du mois : 15
Fréquence : Mensuelle
```

### Étape 4

Le frontend envoie les données au backend.

### Étape 5

Le backend crée l’entente dans la base de données.

### Étape 6

Le backend génère automatiquement toutes les tâches mensuelles.

### Étape 7

Le backend applique les règles du système expert.

### Étape 8

Le frontend affiche les tâches dans le calendrier.

### Étape 9

Si une tâche est en retard, elle apparaît en rouge.

### Étape 10

L’utilisateur peut cliquer sur une tâche et la marquer comme complétée.

---

## 23. Exemple de cas de test obligatoire

Les étudiants doivent tester le scénario suivant.

### Cas de test 1 — Création d’une facture mensuelle

Données :

```text
Titre : Payer la facture mensuelle d’électricité
Montant : 120 $
Date de début : 2026-06-15
Date de fin : 2027-06-15
Fréquence : mensuelle
```

Résultat attendu :

```text
13 tâches sont créées.
Chaque tâche est enregistrée dans la base de données.
Chaque tâche apparaît dans le calendrier.
Chaque tâche possède un état expert.
Chaque tâche possède une couleur.
```

---

### Cas de test 2 — Tâche en retard

Données :

```text
Une tâche a une date passée.
Son statut est "en attente".
```

Résultat attendu :

```text
La tâche devient "en retard".
La couleur devient rouge.
```

---

### Cas de test 3 — Tâche complétée

Données :

```text
L’utilisateur marque une tâche comme complétée.
```

Résultat attendu :

```text
Le statut devient "complétée".
L’état expert devient "terminée".
La couleur devient verte.
```

---

### Cas de test 4 — Tâche urgente

Données :

```text
Une tâche arrive dans les 7 prochains jours.
Elle n’est pas complétée.
```

Résultat attendu :

```text
L’état expert devient "urgent".
La couleur devient orange.
```

---

### Cas de test 5 — Suppression d’une entente

Données :

```text
L’utilisateur supprime une entente.
```

Résultat attendu :

```text
L’entente est supprimée ou archivée.
Les tâches liées sont supprimées ou archivées selon votre choix technique.
Le calendrier est mis à jour.
```

---

## 24. Documentation obligatoire

Le projet doit contenir un fichier README clair.

Le README doit expliquer :

```text
Le nom du projet
Le contexte
Les fonctionnalités
Les technologies utilisées
L’architecture
Le schéma de la base de données
Les règles du système expert
Les routes API
Les étapes d’installation
Les étapes d’exécution
Les captures d’écran
Les difficultés rencontrées
Les améliorations possibles
```

---

## 25. Structure minimale du README

```text
# Système expert de gestion des tâches récurrentes

## 1. Description du projet

## 2. Fonctionnalités principales

## 3. Architecture de l’application

## 4. Technologies utilisées

## 5. Modèle de base de données

## 6. Règles du système expert

## 7. Routes API

## 8. Installation du projet

## 9. Exécution du projet

## 10. Captures d’écran

## 11. Tests réalisés

## 12. Difficultés rencontrées

## 13. Améliorations futures
```

---

## 26. Livrables attendus

Les étudiants doivent remettre :

```text
Le code source complet
Le frontend
Le backend
Le script ou schéma de base de données
Le README
Les captures d’écran
Une courte démonstration
Une explication des règles IF / ELSE
Une preuve que les données sont sauvegardées
```

Le projet doit être remis sous forme de dépôt Git ou d’archive structurée.

---

## 27. Structure de dossier recommandée

Exemple :

```text
smart-reminder-expert-system/
│
├── frontend/
│   ├── src/
│   ├── public/
│   └── package.json
│
├── backend/
│   ├── src/
│   ├── routes/
│   ├── services/
│   ├── models/
│   └── package.json
│
├── database/
│   ├── schema.sql
│   └── seed.sql
│
├── screenshots/
│
└── README.md
```

Autre exemple avec Python :

```text
smart-reminder-expert-system/
│
├── frontend/
│
├── backend/
│   ├── main.py
│   ├── database.py
│   ├── models.py
│   ├── routes.py
│   └── expert_rules.py
│
├── database/
│
└── README.md
```

---

## 28. Présentation orale

Chaque équipe devra présenter son projet.

La présentation doit couvrir :

```text
Le problème traité
La démonstration de l’application
La création d’une tâche récurrente
La génération automatique des tâches
L’affichage dans le calendrier
Le fonctionnement du backend
La base de données
Les règles du système expert
Les difficultés rencontrées
Les améliorations possibles
```

Chaque membre de l’équipe doit être capable d’expliquer une partie du projet.

---

## 29. Barème d’évaluation

### 1. Fonctionnalités CRUD — 20 points

```text
Création d’une entente : 5 points
Lecture et affichage des ententes : 5 points
Modification d’une entente ou d’une tâche : 5 points
Suppression ou archivage : 5 points
```

---

### 2. Génération automatique depuis le backend — 15 points

```text
Génération correcte des tâches mensuelles : 8 points
Sauvegarde en base de données : 4 points
Gestion correcte des dates : 3 points
```

---

### 3. Système expert avec règles IF / ELSE — 20 points

```text
Règle de retard : 5 points
Règle d’urgence : 5 points
Règle de complétion : 5 points
Règle d’annulation ou d’archivage : 5 points
```

---

### 4. Calendrier et interface utilisateur — 15 points

```text
Calendrier clair : 5 points
Couleurs correctes : 5 points
Interface utilisable : 5 points
```

---

### 5. Backend et API — 10 points

```text
Routes API fonctionnelles : 5 points
Séparation claire backend/frontend : 5 points
```

---

### 6. Base de données — 10 points

```text
Modèle de données cohérent : 5 points
Relation entente/tâches correcte : 5 points
```

---

### 7. Documentation et présentation — 10 points

```text
README clair : 4 points
Captures d’écran : 2 points
Explication des règles : 2 points
Présentation orale : 2 points
```

---

## 30. Total

```text
Total : 100 points
```

---

## 31. Améliorations possibles

Les équipes qui terminent les exigences minimales peuvent ajouter des fonctionnalités avancées.

Exemples :

```text
Authentification utilisateur
Notifications par courriel
Notifications navigateur
Filtrage par statut
Filtrage par mois
Recherche par titre
Export CSV
Tableau de bord statistique
Historique des paiements
Mode sombre
Déploiement complet en ligne
Tests unitaires
Dockerisation
Gestion de plusieurs utilisateurs
Système de priorité
Rapport mensuel automatique
```

---

## 32. Résultat attendu

À la fin du projet, vous devez avoir une application fonctionnelle capable de :

```text
Créer une tâche récurrente
Générer automatiquement plusieurs tâches depuis le backend
Sauvegarder les données dans une base de données
Afficher les tâches dans un calendrier
Appliquer des règles IF / ELSE
Colorier les tâches selon leur état
Modifier les tâches
Supprimer les tâches
Marquer les tâches comme complétées
Afficher les tâches urgentes
Afficher les tâches en retard
```

---

## 33. Conclusion

Ce projet vous permet de développer une application complète qui ressemble à un vrai outil professionnel.

Même si l’application semble être un simple calendrier de tâches, elle contient une logique de système expert, car elle prend automatiquement des décisions à partir de règles.

Le backend joue un rôle central : il génère les tâches, applique les règles, sauvegarde les données et fournit les résultats au frontend.

Le projet permet donc de pratiquer plusieurs compétences importantes :

```text
Développement frontend
Développement backend
Base de données
API REST
CRUD
Règles IF / ELSE
Calendrier interactif
Architecture logicielle
Documentation technique
Présentation professionnelle
```

Votre objectif est de livrer une application claire, fonctionnelle, bien structurée et capable de démontrer une vraie logique de décision.
